#!/bin/bash

# SSH and git
if [[ ! -f ~/.ssh/id_rsa ]] ; then
    echo "Create id_rsa: "
    ☣️🍎💾ssh-keygen -t rsa -C 'eddiejnolan@gmail.com'
    🍏💠ssh-keygen -t rsa -C 'enolan@maystreet.com'
fi
if [[ ! -f ~/.ssh/id_rsa_git ]] ; then
    echo "Create ~/.ssh/id_rsa_git: "
    ☣️🍎💾ssh-keygen -t rsa -C 'eddiejnolan@gmail.com'
    🍏💠ssh-keygen -t rsa -C 'enolan@maystreet.com'
fi
