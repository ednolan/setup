#!/usr/bin/env bash

brew help &>/dev/null
[[ $? -eq 0 ]] || /usr/bin/env bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"

brew install gnu-getopt htop tmux wget fzf
